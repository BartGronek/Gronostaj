#include <iostream>
int main()
{
    int ile;
    
    cout << "Ile razy wypisac napisy? ";
    cin >> ile;
    if( ile > 0 )
    do
    {
        std::cout << "Napis" << std::endl;
        ile--;
    } while( ile > 0 );
    
    return 0;
}
